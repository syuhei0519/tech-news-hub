root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/api-gateway ./cmd/api-gateway"
bin = "./tmp/api-gateway"
delay = 200
exclude_dir = ["tmp", "vendor"]
include_ext = ["go"]
stop_on_error = true

[log]
time = true
